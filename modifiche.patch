diff --git a/processor/config.py b/processor/config.py
index 943338f..9a4b8f1 100644
--- a/processor/config.py
+++ b/processor/config.py
@@ -22,6 +22,8 @@ class Config:
     enable_hdr: bool
     video_crf: int
     mirage_upload_crf: int
+    color_correction_gamma: float = 1.0
+    color_correction_saturation: float = 1.0
     upscale_output: bool = True
     mirage_base_url: str = "https://api.mirage.app/v1"
 
@@ -58,6 +60,8 @@ def load_config(config_path: Path) -> Config:
         enable_hdr=bool(raw.get("enable_hdr", True)),
         video_crf=int(raw.get("video_crf", 20)),
         mirage_upload_crf=int(raw.get("mirage_upload_crf", 24)),
+        color_correction_gamma=float(raw.get("color_correction_gamma", 1.0)),
+        color_correction_saturation=float(raw.get("color_correction_saturation", 1.0)),
         upscale_output=bool(raw.get("upscale_output", True)),
         mirage_base_url=str(raw.get("mirage_base_url", "https://api.mirage.app/v1")),
     )
diff --git a/config.yaml.example b/config.yaml.example
index 8e52723..9138e76 100644
--- a/config.yaml.example
+++ b/config.yaml.example
@@ -42,10 +42,19 @@ output_width: 2160
 output_height: 3840
 
 # Upscale Mirage output to output_width x output_height and convert to HLG.
-# true  → re-encode with libx265, zscale SDR→HLG, output shows "4K HLG" in players
+# true  → re-encode with libx265, optional SDR brightness correction,
+#         zscale SDR→HLG, output shows "4K HLG" in players
 # false → remux only (stream-copy video), inject HLG container tags, no re-encode
 #         Use false if you want zero extra quality loss and don't need 4K metadata.
 upscale_output: true
+
+# Optional post-Mirage color correction applied during FINALIZE before SDR→HLG.
+# Keep both at 1.0 to disable correction.
+# Example values from screenshot analysis:
+#   gamma: 0.53
+#   saturation: 1.05
+# This is a cosmetic correction for Mirage SDR output, not true HDR recovery.
+color_correction_gamma: 1.0
+color_correction_saturation: 1.0
 
 # CRF for the final upscale+HLG encode (only used when upscale_output: true)
 # Lower = higher quality, larger file. 18 is a good starting point for 4K HEVC.
diff --git a/processor/ffmpeg_utils.py b/processor/ffmpeg_utils.py
index 94c8415..39b8dca 100644
--- a/processor/ffmpeg_utils.py
+++ b/processor/ffmpeg_utils.py
@@ -139,7 +139,24 @@ def _trim_filter_chain(width: int, height: int) -> str:
     return f"{scale},format=yuv420p"
 
 
-def _sdr_to_hlg_filter_chain(width: int, height: int) -> str:
+def _color_correction_filter(gamma: float, saturation: float) -> str:
+    """Return optional FFmpeg eq filter for post-Mirage SDR correction."""
+    gamma = max(gamma, 0.01)
+    saturation = max(saturation, 0.0)
+
+    if abs(gamma - 1.0) < 1e-6 and abs(saturation - 1.0) < 1e-6:
+        return ""
+
+    return f"eq=gamma={gamma}:saturation={saturation},"
+
+
+def _sdr_to_hlg_filter_chain(
+    width: int,
+    height: int,
+    *,
+    color_correction_gamma: float = 1.0,
+    color_correction_saturation: float = 1.0,
+) -> str:
     """Upscale Mirage SDR output and convert pixels to HLG (BT.2020 / arib-std-b67).
 
     Mirage returns bt709 SDR; zscale performs the actual SDR→HLG conversion.
@@ -147,8 +164,10 @@ def _sdr_to_hlg_filter_chain(width: int, height: int) -> str:
     """
     scale = _scale_filter(width, height)
     return (
-        f"{scale},"
-        "zscale=rangein=full:range=limited:"
+        f"{scale},"
+        f"{_color_correction_filter(color_correction_gamma, color_correction_saturation)}"
+        "zscale="
+        "rangein=full:range=limited:"
         "primariesin=bt709:primaries=bt2020:"
         "matrixin=bt709:matrix=bt2020nc:"
         "transferin=bt709:transfer=arib-std-b67,"
@@ -352,6 +371,8 @@ def remux_and_upscale(
     output_height: int,
     video_crf: int,
     upscale: bool = True,
+    color_correction_gamma: float = 1.0,
+    color_correction_saturation: float = 1.0,
     progress: Optional[Progress] = None,
     task_id: Optional[TaskID] = None,
 ) -> list[str]:
@@ -370,14 +391,28 @@ def remux_and_upscale(
             warnings.append(
                 f"Upscaling {src_w}x{src_h} -> {output_width}x{output_height} "
-                f"+ zscale SDR→HLG + hevc_metadata (9-18-9)."
+                f"+ optional color correction + zscale SDR→HLG + hevc_metadata (9-18-9)."
             )
         else:
             warnings.append(
-                "Resolution already at target; applying zscale SDR→HLG + hevc_metadata (9-18-9)."
+                "Resolution already at target; applying optional color correction "
+                "+ zscale SDR→HLG + hevc_metadata (9-18-9)."
             )
 
-        video_filter = _sdr_to_hlg_filter_chain(output_width, output_height)
+        if (
+            abs(color_correction_gamma - 1.0) >= 1e-6
+            or abs(color_correction_saturation - 1.0) >= 1e-6
+        ):
+            warnings.append(
+                "Applying post-Mirage SDR correction during FINALIZE: "
+                f"gamma={color_correction_gamma:.3f}, "
+                f"saturation={color_correction_saturation:.3f}"
+            )
+
+        video_filter = _sdr_to_hlg_filter_chain(
+            output_width,
+            output_height,
+            color_correction_gamma=color_correction_gamma,
+            color_correction_saturation=color_correction_saturation,
+        )
         duration = get_duration_seconds(input_path)
         cmd = [
             "ffmpeg",
diff --git a/processor/batch.py b/processor/batch.py
index e9a6350..48e6325 100644
--- a/processor/batch.py
+++ b/processor/batch.py
@@ -402,6 +402,8 @@ def process_folder(
                     output_height=config.output_height,
                     video_crf=config.video_crf,
                     upscale=config.upscale_output,
+                    color_correction_gamma=config.color_correction_gamma,
+                    color_correction_saturation=config.color_correction_saturation,
                     progress=fin_progress,
                     task_id=fin_task,
                 )
